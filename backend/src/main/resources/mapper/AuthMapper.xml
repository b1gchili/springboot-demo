<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.example.student.mapper.AuthMapper">

    <select id="findEnabledUserByUsername" resultType="org.example.student.model.UserRecord">
        SELECT user_id, username, password, phone, display_name
        FROM sys_user
        WHERE username = #{username}
          AND enabled = 1
    </select>

    <select id="findEnabledUserByPhone" resultType="org.example.student.model.UserRecord">
        SELECT user_id, username, password, phone, display_name
        FROM sys_user
        WHERE phone = #{phone}
          AND enabled = 1
    </select>

    <update id="updateLastLoginTime">
        UPDATE sys_user
        SET last_login_time = NOW()
        WHERE user_id = #{userId}
    </update>

    <insert id="insertSmsCode">
        INSERT INTO sms_code (phone, code, expires_at, used)
        VALUES (#{phone}, #{code}, #{expiresAt}, 0)
    </insert>

    <update id="expireAvailableSmsCodes">
        UPDATE sms_code
        SET used = 1
        WHERE phone = #{phone}
          AND used = 0
    </update>

    <select id="findLatestAvailableSmsCode" resultType="org.example.student.model.SmsCodeRecord">
        SELECT id, phone, code, expires_at
        FROM sms_code
        WHERE phone = #{phone}
          AND used = 0
        ORDER BY create_time DESC
        LIMIT 1
    </select>

    <update id="markSmsCodeUsed">
        UPDATE sms_code SET used = 1 WHERE id = #{id}
    </update>
</mapper>
